<view>
    <image wx:if="{{list.length > 0}}" wx:for="{{list}}" wx:key="index" src="{{item}}"/>
    <view class="no-guide" wx:if="{{list.length <= 0}}">
        <text class="no">暂无门店指引</text> 
    </view>
</view>
